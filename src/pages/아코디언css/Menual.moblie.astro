---
import Buy from '../../../styles/menual/buy.png'
import Message from '../../../styles/menual/message.png'
import Visit from '../../../styles/menual/visit.png'
import Result from '../../../styles/menual/result.png'
import BackgroundImg from '../../../styles/menual/background.png'

const menualList = [
  {
    name: '검진상품 선택 및 구매하기',
    img: Buy,
    list: [
      {
        main: '관련 증상이나 질환을 참고로 검진 상품을 선택하세요. \n(예시 : 당뇨, 빈혈 등)',
      },
      {
        main: '상품 상세페이지에서 희망하는 검사소 지역을 참고하세요.',
      },
    ],
  },
  {
    name: '안내 문자 확인 ',
    subName: '(휴대폰, e-mail)',
    img: Message,
    list: [
      {
        main: '추가 내용 확인 필요 : 영수증, 바우처, 검사 시 주의 사항, 검사소 위치 설명, e티켓 등',
      },
      {
        main: '검사소 찾기 : e 티켓 넘버와 함께 가까운 검사소 정보를 문자로 안내해드립니다.\n보다 많은 검사소 위치는 Labcorp 공식 사이트에서도 확인하실 수 있습니다.',
        sub: '(미국내 2,000여개)',
      },
    ],
  },
  {
    name: '검사소 방문하기',
    img: Visit,
    list: [
      {
        main: '원하시는 날짜, 시간 및 검사소를 방문하시면 기다리지 않고 검사를 진행하실 수 있습니다.',
        sub: '(상세페이지에 운영시간 및 가까운 검사소 위치를 확인하세요.)',
      },
      {
        main: '단, 예약을 원하실 경우 Labcorp 공식 사이트에서 가능합니다.',
        sub: '(한국어 안내: 링크)',
      },
      {
        main: '방문하신 검사소의 키오스크(무인단말기)에 이름과 e 티켓 넘버 기입 및 각종 동의서 서명',
      },
      {
        main: '고객 이름 호명 후 현장 직원의 안내에 따라 채혈 후 귀가',
      },
    ],
  },
  {
    name: '검사 결과 확인하기',
    img: Result,
    list: [
      {
        main: '휴대폰 문자 안내에 따라 웰코치몰에서 검진 결과 리포트 확인하기',
      },
      {
        main: '검진결과가 반영된 맞춤 영양관리 시작하기',
      },
    ],
  },
]
---

<section
  w-flex="~ col"
  w-items="between"
  w-w="full"
  w-h="full"
  w-pos="relative"
>
  <img
    class="section0"
    w-pos="absolute top-0 bottom-0"
    w-w="full"
    w-h="full"
    w-object="cover"
    w-opacity="27"
    src={BackgroundImg}
  />
  <div
    w-p="x-1/20"
    w-m="x-auto t-85px"
    w-container="~"
    w-h="full"
    w-z="40"
    w-flex="~ col"
  >
    <ol class="menu-wrap" w-flex="~ col" w-h="full" w-m="b-20px">
      {menualList.map((menu, i) => (
        <li
          class="out"
          w-cursor="pointer"
          w-bg="white"
          w-flex="~"
          w-justify="between"
          w-items="center"
          w-p="5"
          w-h="1/10"
          w-transition="duration-100"
          w-shadow="custom"
          w-text="[#6E6E6E] center"
        >
          <span w-text="1.375rem">{i + 1}</span>
          <p>
            {menu.name}
            <span w-text="1.125rem <lg:0.75rem">{menu.subName}</span>
          </p>
          <img
            src={menu.img}
            w-h="min-42px max-55px"
            w-w="min-40px max-56px"
            w-p="x-1"
          />
        </li>
        <ol w-m="t-2" w-p="x-5 y-3" w-bg="[#F5F5F5]" class="acodian">
          {menu.list.map(data=>(
            <li w-text="0.75rem justify" w-font="leading-[2.0]"  w-flex="~ col inline-block"  w-whitespace="pre-line" w-h="auto"  >
              <span>- {data.main}</span><span w-text="[#444444] 0.625rem">{data.sub}</span>
             </li>
            ))
          }
          </ol>
      ))}
    </ol>
  </div>
</section>
<script>
  const menuWrap = document.querySelector('.menu-wrap')
  const detailWrap = document.querySelector('.out')

  const selectMenu = (ulEl, liEl) => {
    Array.from(ulEl.children).forEach((v) => v.classList.remove('selected'))
    if (detailWrap) liEl.classList.add('selected')
  }

  menuWrap.addEventListener('click', (e) => {
    const selected = e.target
    selectMenu(menuWrap, selected)
  })
</script>

<style>
  .section0 {
    object-position: center -10%;
  }
  .out:hover {
    outline: 3px solid #33333399;
    outline-offset: -3px;
  }
  .menu-wrap li.selected {
    color: black;
  }
  .menu-wrap ol.selected {
    display: none;
    color: red;
  }
  .menu-wrap li > * {
    pointer-events: none;
  }
</style>
